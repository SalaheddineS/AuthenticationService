name: CI

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      spring.data.mongodb.uri: mongodb+srv://salaheddine2000:salaheddine2000@cluster0.o97vm5q.mongodb.net/SpringBoot2?retryWrites=true&w=majority
      spring.data.mongodb.username: salaheddine2000
      spring.data.mongodb.password: salaheddine2000
      spring.data.mongodb.database: SpringBoot2

      jwt.secret: ThIsIsMySecrEtkEeEyAndImUsingItToSignMyJwtTokenHEHEHEbBoI
    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up JDK 17
      uses: actions/setup-java@v3.10.0
      with:
        java-version: '17'
        distribution: 'adopt'

    - name: Build with Maven
      run: mvn package --file pom.xml

    - name: Run tests
      run: mvn test --file pom.xml
